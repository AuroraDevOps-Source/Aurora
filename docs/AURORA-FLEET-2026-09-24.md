# Tenant fleet and order filters — September 24, 2026

`bbatts` already belongs to the isolated demo tenant Aurora Demo Co (`11111111-1111-1111-1111-111111111111`). Its existing membership was retained, preserving the test orders, manifests, and shared-product logins. The workspace now displays the authenticated tenant name. No second tenant or alternate default membership was created.

Fleet Management uses the existing PostgreSQL `routing_equipment_type` and `routing_equipment_unit` tables and their forced tenant RLS. A routing-truck flag distinguishes actual planning trucks from the older reference equipment samples. New trucks have ID, display name, terminal, type, VIN, plate/state, availability, default shift start/end, explicit UTC offset, and maintenance notes. Equipment types hold reusable capacities, categories, routing profiles and operating costs. Power-only tractors cannot be selected as a complete routing truck. Drivers and hours-of-service enforcement remain future work.

Six synthetic trucks from the appointment fixture are saved as actual fleet records, with their original capacities/rates/categories and shifts. The existing 279 reference units remain stored but are not automatically offered as routing trucks. A new Orders draft uses only available, enabled trucks belonging to the authenticated tenant and the orders' terminal. At submission the server checks those truck IDs again and applies saved capacity/profile/cost values; the wizard controls daily availability and working hours. No eligible trucks produces a clear error rather than silently using file vehicles. The file still supplies the order plan's date and depot context. Completed manifests preserve their original route snapshots. Legacy standalone file planning remains available separately at `/routing`.

The Orders import selector was removed. The initial From/To range is today 00:00:00–23:59:59 in browser local time. Clear filters includes older dates. The current desktop test orders retain their September 1 dates; they were not silently rescheduled.

Status vocabulary follows `D:/Development/Nova 2.0/src/Nova.Api/Domain/Entities.cs` (`ShipmentStatus`): Pending, ReadyToRoute, AwaitingAppointment, Routed, Dispatched, InTransit, OutForDelivery, Delivered and Voided. Aurora displays ReadyToRoute as **Ready to Ship**, per request. Only that state is selectable. Finish transitions scheduled orders to Routed. Migration 0013 translates existing Available/Manifested values and updates the database constraint/default. Later dispatch/delivery transitions are not implemented in this mini TMS yet. The top-level Aurora TMS label is now Nova 2.0; its route and SSO identity stay unchanged.

Validation: Release build and publishing succeed; 33 workspace/fleet checks passed against an isolated PostgreSQL database (including RLS, saved-fleet sourcing, unavailable/cross-terminal rejection, overnight shifts and saved costs). 31 planning session and 85 routing regression checks passed. The isolated database was removed after verification. Deployment release: 20260924-4, with a pre-migration database backup and preserved tenant/product links.

Release archive SHA256: `1b8dfd692fc2e860fe0cf5a9d77158c7e0b940f11887d14be6b864e2dea0a426`. Pre-migration backup: `/opt/aurora-auth/private/equipment-pre-20260924-4-20260924-060918.dump`. Public health, login/SSO, tenant context, demo truck persistence and creation of a draft from the saved fleet passed after deployment. No additional paid optimization was submitted for this release.
