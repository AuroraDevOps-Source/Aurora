@page "/"
@attribute [Authorize]
@inject NavigationManager Navigation

@code {
    // The product header is the navigation now; land on Aurora's own workspace.
    protected override void OnInitialized() => Navigation.NavigateTo("aurora/orders", replace: true);
}
